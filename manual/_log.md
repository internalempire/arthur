# Log

**2026-08-16 — Pericardial capacity and cardiac tamponade.** The fixed 430 mL pericardial knee became a user control while the existing nonlinear pressure relation and normal default were retained. The new tamponade preset reduces available capacity, generating shared diastolic pressure, preferential loss of RV filling and depressed output; restoring capacity is an executable decompression experiment. A dedicated page and model-generated pressure–volume figure state the deliberate boundary: capacity is not effusion volume, and the model does not claim a calibrated pulsus-paradoxus threshold.

> Append-only record of what was done to the manual and when. Newest last.

**2026-08-14 — Viewer built.** Single-page shell over plain markdown: hash router, sidebar from `manifest.json`, in-page table of contents, previous/next, search over titles and summaries, light and dark schemes. `marked` and KaTeX are vendored into `manual/vendor` (960K, no external requests). zero-md was evaluated and not used: it resolves seven packages from a CDN at runtime, and its shadow DOM would have fought the surrounding layout.

**2026-08-14 — Renderer shared with the linter.** Markdown and maths rendering moved to `render.mjs` so `tools/lint.mjs` verifies pages through the same code the browser runs. Maths is tokenised before markdown parses, because running KaTeX over parsed HTML lets marked mangle the LaTeX first.

**2026-08-14 — First page.** `pulmonary-vascular-resistance` written, merging what the plan had as two pages. Chosen as the first because it exercises every convention at once: an implementation worth explaining, a reversal to account for, coefficients that cannot be cited, and real clinical limits.

**2026-08-14 — Conventions recorded.** Two rules in `_schema.md` come from defects that shipped: display-math delimiters alone on their line, and one line per paragraph. Both files had passed a source-level check.

**2026-08-14 — Dev server.** `tools/serve.mjs` now serves directory indexes, so the manual answers at `/manual/`, and takes its port from `PORT`.

**2026-08-14 — Stage 2: physiological foundations.** Eight pages written: transmural pressure, pleural pressure, abdominal pressure, venous return, vascular waterfalls, ventricular interdependence, ventriculo-arterial coupling, and the four effects of a breath. Every quantity quoted was measured by running the model rather than recalled.

**2026-08-14 — Guyton figure added.** `figure/generate.mjs` now also draws the venous return and cardiac function curves at two levels of PEEP, using the same `venousReturnCurve` and `cardiacFunctionCurve` the app plots. A first version evaluated them against the instantaneous state and its crossing disagreed with the integrator by 5%; it now uses the cycle-mean operating point, and mean right atrial pressure matches.

**2026-08-14 — Two findings worth recording.** The pericardium does nothing below 430 mL of total heart volume, so most scenarios never engage it, and even a frankly dilated heart loses under 2% of output to it. And isolating any single coupling requires disabling the baroreflex first, or the reflex absorbs the effect and the experiment measures compensation.

**2026-08-14 — Symbols must be defined.** New convention in `_schema.md`: every symbol in a formula gets a definition list under it, naming what it is and its units. Retrofitted to all ten pages already written. Prompted by the elastance formula on the coupling page, where a reader had to infer from context that $P$ was a pressure and $E$ an elastance.

**2026-08-14 — `_todo.md` added.** Planned work, so an acknowledged gap is not mistaken for an oversight. First entry is a cardiac tamponade scenario and the pericardial volume control it would need.

**2026-08-14 — The waterfall asymmetry, stated properly.** The venous side uses one closing pressure where the superior and inferior caval routes see different surrounding pressures, while the pulmonary side splits the bed. The rewritten section says what the model therefore cannot show, and admits the asymmetry survives because only the pulmonary simplification broke an executable check — a statement about test coverage, not about defensibility.

**2026-08-14 — Lint rule widened.** A link to a machinery page (`_todo.md`, `_schema.md`) is valid if the file exists, not only if the slug is in the manifest. Verified that genuinely broken links still fail.

**2026-08-14 — Stage 3: respiratory mechanics.** Seven pages: equation of motion, pressure–volume curve, two-population lung, recruitment and R/I, hysteresis, stress index, expiratory flow limitation.

**2026-08-14 — Two more figures.** The lung pressure–volume curve at three compliances, and airway pressure through a constant-flow inspiration in a recruiting, a linear and a distending lung — the last carrying the model's own fitted stress indices of 0.72, 1.03 and 1.80. A shared chart helper now backs three of the four figures. Its first output was invalid XML: a label reading "SI < 1" put a raw `<` into the document, so figure text is escaped now.

**2026-08-14 — The stress-index figure was understating SI < 1.** Reported by Nicola from the rendered page, and correct. The three cases shared an absolute pressure axis on which the distending breath rises 63 cmH₂O and the recruiting one 10, so the smaller curvature occupied about 1% of the plot height while the larger occupied 12%. The curvature was real throughout — measured as deviation from a straight line at mid-inspiration, +8.6% of the rise for the recruiting case against −16.0% for the distending one. Both axes are now normalised to each breath's own rise, and at mid-inspiration the three curves sit at 58.3%, 49.0% and 33.7%. A plotting fault, not a model fault.

**2026-08-14 — Bedside reading of the stress index.** New section and figure: one real volume-controlled breath, unnormalised, with the fitted power law and a straight rise drawn over it, and the excluded resistive onset marked. Also recorded a limit found while building it — volume control here runs constant flow for the whole inspiratory time and has no pause, so the reported plateau is computed rather than measured during a hold.

**2026-08-14 — Hysteresis figure.** The two limbs as volume against transpulmonary pressure, opening at 20 and closing at 12 cmH₂O, drawn from `openBand` so the loop is the band the play operator actually uses.

**2026-08-14 — Chart helper gained a right margin and dashed series.** Legend labels were being clipped at the fixed 150 px right pad.

**2026-08-14 — The hysteresis figure was drawing the wrong thing.** Reported by Nicola against the classical loop, and the criticism was structural rather than cosmetic. The figure sampled `openBand` at each pressure independently, which gives the two edges of the band — the extreme states the lung *could* be in — not a path. A hysteresis loop is a trajectory. It is now an actual inflation to 35 cmH₂O and back, carried through `stepOpenFraction` at every step.

**2026-08-14 — Which exposed a real defect.** The redrawn loop closes at the top and not at the bottom: 0.36 L inflating against 0.72 L deflating at zero transpulmonary pressure. The cause is that `openFractionAt` applies the `pOpen` − `pClose` gap to *both* unit populations, so normal units close only below −8 cmH₂O and the whole undiseased share stays open at zero. The gap is a statement about recruitable units and applying it to the rest of the lung is a side effect. Recorded in `_todo.md` with both candidate fixes and the reason neither is obviously right; the tidal range is unaffected, which is why it had gone unnoticed.

**2026-08-14 — Stress-index figure redrawn in the form a ventilator shows.** Three panels — normal, over-distension, tidal recruitment — each a full breath scaled to its own pressure range, with the fitted segment highlighted, carrying the model's own indices of 1.03, 1.80 and 0.72. This replaces both the normalised comparison and the single-breath-with-fit figure: three independently scaled panels solve the shared-axis problem outright, which is why the classical figure is drawn that way.

**2026-08-14 — Hysteresis: the feature does not do what the page claimed.** Nicola rejected a second hysteresis figure against the textbook loop, and checking why turned up more than a drawing fault.

Two findings. The loop cannot close at the bottom at any physiological pressure — the `pOpen` − `pClose` gap shifts both unit populations, so normal units stay open below zero transpulmonary pressure and the limbs converge only at −12 cmH₂O, at the numerical floor. And, more seriously, an incremental followed by a decremental PEEP trial retains **no** recruitment at all: the limbs are identical to three decimal places at every opening pressure from 20 to 50 cmH₂O. `stepOpenFraction` re-clamps the state into the band every step, and each expiration drags it back onto the deflation limb at that breath's minimum pressure, so memory is destroyed once per breath.

What the model does deliver is a level shift — a cycled lung sits on its deflation limb, up to 21 percentage points more open at the closing pressure — which is real and teachable. The page's earlier table showed exactly that and described it as what a manoeuvre leaves behind, which was wrong. Page rewritten, summary line corrected, the rationale section now records that choosing a mechanism which could in principle produce the behaviour, without checking that it did, is the error. Both defects are in `_todo.md` with candidate fixes.

The figure is now the level shift against PEEP rather than a pressure–volume loop, because a loop drawn from this model would claim a path dependence it does not have.

**2026-08-14 — Retraction: the hysteresis feature does have memory.** The previous entry claimed an incremental/decremental PEEP trial retained nothing and that the play operator lost its state every breath. That was wrong, and a review by another model caught it.

The claim rested on one protocol — PEEP 4 → 24 → 4 at 400 mL with `pClose` = `pOpen` − 8 — which fails for two independent reasons, both already documented in `tests/suites/recruitment.test.mjs` and both reproducible: a 400 mL breath reaches the opening limb by itself, so a manoeuvre adds 2.3 points instead of 15.9; and a final PEEP of 4 puts end-expiratory transpulmonary pressure far below every closing pressure tried, so the lung shuts on the way down. Sweeping `pOpen` from 20 to 50 while holding the gap at 8 kept both faults fixed, which is why the sweep looked conclusive.

Run the protocol the tests use — 250 mL, `pOpen` 22, `pClose` 6, PEEP 10 → 35 → 10 — and the lung goes from 80.3% to 96.2% open at the same PEEP, with the resistance coefficient falling from 1.41 to 1.21 WU. The limbs separate properly against transpulmonary pressure and converge above 20 cmH₂O.

Two process failures, not one. Designing a protocol whose settings guarantee the null result and then generalising from it is the mirror of the postmortem's "tests that passed for the wrong reason". And the existing suite already tested this, green, in the file the page is about — checking it would have taken a minute.

Page rewritten: retitled *Recruitment hysteresis*, since the model represents recruitment/derecruitment hysteresis and not tissue hysteresis, and the inability to draw a classical pressure–volume loop is therefore out of scope rather than a defect. The figure is now the incremental and decremental limbs of one continuous state plotted against end-expiratory transpulmonary pressure — the previous "hysteresis off versus on against PEEP" compared two model configurations rather than two paths, and conflated PEEP with the transpulmonary pressure `pClose` is defined in. Postmortem narrative removed from the page and kept here.

The one defect that survives review is real and unchanged: the `pOpen` − `pClose` shift is applied to both unit populations. `_todo.md` now carries the reviewer's proposed separation of the stateful recruitable fraction from the single-valued normal one.

**2026-08-14 — Physiological documentation audit.** Corrected unit conversions and variable definitions in abdominal pressure and vascular waterfalls; aligned the EFL equation with the actual zero-PEEP relaxation reference; separated fluid responsiveness from the venous-return plateau; relabelled MAP/SV as a load surrogate rather than effective arterial elastance; and qualified stress-index, PVR, pleural-pressure and breath-timing claims that were stronger than the implementation or literature supports. The hysteresis figure is now described explicitly as recruitment state against end-expiratory transpulmonary pressure, not as a classical pressure–volume loop. Structural issues requiring code decisions were consolidated in `_todo.md` rather than changed silently.

**2026-08-14 — Recruitment memory separated from already-aerated lung.** The hysteretic state now contains only the collapsed but recruitable share. Already-aerated lung follows current transpulmonary pressure and no longer inherits the `pOpen`–`pClose` gap. The pressure inversion was updated so this separation is respected within each respiratory step, and the PVR/recruitment metrics read the same state. Tests now require an uncollapsed lung to have no hysteresis and to remain bit-identical with the feature on or off.

The hysteresis page was rewritten clinician-first: opening and closing controls are described as the centres of gradual pressure ranges, the implementation formula follows the physiological explanation, and the classical respiratory P–V loop is explicitly kept out of scope. The figure retains all sampled points but uses padded, truncated axes so the clinically relevant separation occupies the panel rather than the upper third of an empty 0–100% scale.

**2026-08-14 — Clinical scenarios consolidated.** The nine planned scenario pages were replaced by one cross-linked `scenarios` page covering all eleven active presets, how to interrogate them and where each becomes non-interpretable. The weaning preset was retired because a matched comparison did not reproduce its claimed haemodynamics and the model omits too many determinants of weaning-induced pulmonary oedema. The LV-failure preset is now an explicitly afterload-dominant experiment: PEEP 0 → 10 lowers transmural end-systolic pressure, reduces ESV more than EDV and raises respiratory-cycle-averaged output. The page and decision log state that the selected stiff thoracic envelope is part of the demonstration, not a general property of cardiogenic pulmonary oedema.

**2026-08-14 — Large-swing scenario made interpretable.** The former `swing-no-variation` preset taught from PPV despite correctly marking PPV unavailable during spontaneous effort. It is now `swing-limited-reserve`: the same vigorous spontaneous phenotype contrasts a >20 cmH₂O pleural swing with a non-steep local Guyton preload slope. The manual, README and validation audit no longer quote the hidden waveform value or imply that the scenario validates a dynamic arterial index.

**2026-08-14 — Pulmonary transit made state-dependent.** The fixed 2.0 s buffer was replaced by a central-volume estimate based on represented PA-to-LA blood volume and mean RV output. The eight stages retain a bounded share of that time and adapt over 2 s, preserving the reference two-to-three-beat ordering while lengthening transit in matched embolic and congested low-output phenotypes. A dedicated clinician-first page now separates whole-circuit transit, staged-buffer time and contrast measurements; fixed-time claims were removed from the synthesis page and `_todo.md`.

**2026-08-14 — Systemic filling and bedside indices documented.** Seven clinician-first pages now cover stressed volume, venous tone, the aggregate baroreflex, interpretability, pulse pressure variation, preload reserve, and Pmsf estimated from occlusion holds. Five figures are regenerated from the current model rather than hand-authored: the venous pressure-volume relation, steady baroreflex response, Guyton preload-reserve sweep, one-breath arterial waveform, and the hold-derived extrapolation. Each page separates physiology, implementation, design rationale, model limits and clinical limits, and the occlusion page records the model's uncalibrated 14.7 mmHg intercept bias rather than presenting it as a human prediction.

**2026-08-14 — Stressed volume and venous tone separated visually.** The shared venous pressure-volume figure concealed the central distinction and labelled compliance as the plotted slope. It has been replaced by two model-generated figures: fluid moves rightward along a fixed relation, whereas tone raises pressure vertically at unchanged reservoir volume while shifting zero-pressure volume from 2,800 to 2,600 mL. Both pages now state that 100 mL/mmHg is compliance and that its slope on pressure-over-volume axes is 0.01 mmHg/mL. The curves begin at their algebraic zero-pressure intercepts rather than graphically flooring an unshown negative-pressure extension.

**2026-08-14 — Manual completed to 48 pages.** Start, pulmonary-circulation, panel, control and artefact sections now have their planned pages. The home page maps every component of the application; operational pages quote the live parameter registry and distinguish interface behaviour from physiology; architecture, validation and global limits define what a model result can support. The old waveform summary was corrected because the panel has no airflow trace.

**2026-08-14 — HPV documented as a surrogate, not an oxygen model.** The new page states that the control raises resistance only in the derecruited parallel pathway and that no alveolar oxygen tension exists. A model-generated figure holds lung volume at FRC and shows how selective tone changes the open-bed flow share. It is labelled as conductance sharing rather than a human dose–response curve.

**2026-08-14 — Central bibliography generated.** `manual/tools/build.mjs` now extracts page-level reference sections, deduplicates DOI, PubMed and author–year–pages identities, and regenerates `bibliography.md`. This preserves local context on concept pages while satisfying the one-place index without a second manually maintained list.

**2026-08-15 — Numerical documentation made executable.** An independent audit found that the EFL table and README stress-index values had survived a model recalibration unchanged. Numerical examples now live in `manual/model-examples.mjs`, which records the complete manoeuvre and regenerates the Markdown blocks from fresh simulations. The same stress-index cases drive the SVG figure. Both `npm test` and `manual:lint` fail when a generated table drifts, and exact test counts were removed from prose because they were another untested value. Reproduction also resolved one false positive in the audit: the manual stress-index table was numerically correct, but its hidden phenotype and ambiguous compliance definition made it impossible to reproduce from the page alone.

**2026-08-15 — Compliance and maximum capacity separated.** `clung` now controls the local slope of aerated tissue and a new `lungCapacity` control sets the completely open ceiling, default 6 L. Collapse still determines the accessible share. The pressure–volume page, control guide, two-population explanation, PVR landmarks, stress-index examples and generated figure were updated together. The previous construction could shrink a low-compliance, collapsed baby lung twice; the new tests require slope and ceiling to vary independently.

**2026-08-15 — README returned to its project-overview role.** The 1,350-line document had become a second manual, duplicating equations, numerical examples, implementation detail and bibliography that could drift independently. It now presents the project's purpose, model domains, interface, active scenarios, validation approach, architecture and main limitations at overview depth, with direct links into the manual for every substantive topic. Documentation contracts now check scenario coverage and manual entry points instead of requiring deep model constants or generated numerical tables in the README.

**2026-08-15 — Wedge and the numerical readout layer made mechanistic.** A new clinician-first PAWP page separates catheter occlusion pressure, atmospheric left atrial pressure and transmural filling pressure; documents the model's three-second left-atrial surrogate and zone 3 pressure-margin heuristic; and adds a generated zone 3/zone 2 mechanism diagram. The numerical-tiles page was rebuilt around the chain from state to measurement, calculation, interpretation and status rather than a shallow inventory. The interface now says “Wedge surrogate” and “zone 3 index”, and derived PVR inherits wedge caution. The long bibliographic footer was removed from the application while the teaching-only warning was retained, and the main layout gained spacing below the sticky header.

**2026-08-15 — Waveform values separated from instantaneous samples.** The right-hand waveform rail now repeats the cycle-level summaries shown in the numerical tiles rather than updating from every animation-frame sample. Systemic and pulmonary arterial pressures are shown as systolic/diastolic values, respiratory traces use plateau pressure and pleural swing, and the pulmonary arterial label is consistently abbreviated PAP.

**2026-08-15 — LLM development provenance made explicit.** The README and manual now state that the model, code, tests, interface and documentation were generated primarily with GPT-5.6-Sol at the high reasoning setting, cross-reviewed by other LLMs and directed and revised by a human clinician using cited literature. Architecture and validation pages distinguish this transparent provenance from independent physiological or clinical validation.

**2026-08-16 — Passive mode transitions made explicit.** Changing interactively from any scenario to volume control or pressure control now clears inherited inspiratory muscle pressure, so a controlled mode begins with a genuinely passive patient. The effort control remains editable and can be raised afterwards for a deliberate mixed ventilator–patient experiment. Pressure support preserves effort. Executable transition contracts prevent spontaneous scenario settings from silently contaminating passive waveform interpretation.

**2026-08-16 — PVR decision notes integrated into existing pages.** Two standalone drafts on the volume axis and the FRC nadir were consolidated into the pulmonary-vascular-resistance and PVR-panel pages rather than added to the manual map. The volume-versus-pressure argument is now presented as a qualified state-descriptor test rather than proof of exclusive causality; pressure-reference and waterfall effects remain explicit. The nadir section now records the four simultaneous contributors to the former excessive ARDS values, the hierarchy from human catheter magnitude to clinical topology to qualitative animal mechanics, and the fact that the FRC minimum is constructed rather than discovered. The Hakim inflation evidence was corrected to the 53:1110 paper instead of being attributed to the preceding 52:710 partition-method paper.

**2026-08-21 — Lung and chest wall made mechanically independent.** The wall now has its own sigmoid relaxation curve, with `ccw` as a local slope and a separate `cwLoad` pressure offset. Passive volume is the intersection of lung and wall recoil rather than a lung-derived point recentered to −5 cmH₂O. The equation-of-motion, pleural-pressure, pressure–volume, Campbell, controls, stress-index, abdominal-pressure and scenario pages were revised together; a model-generated wall-curve figure and an executable equilibrium table show the distinction. The implementation deliberately retains one aggregate wall rather than adding rib-cage and diaphragm compartments.

**2026-08-21 — PPV filling-state limits made explicit.** The PPV page now documents both non-monotonic extremes rather than only the filled-end piston rise. A model-generated two-panel figure shows that severe underfilling can combine low PPV with high preload reserve, while diffuse zone-III conditions can raise PPV again at high filling. The numerical tile now calls PPV descriptive and not a filling-state verdict; executable checks keep that warning, the figure and the known dry-end discordance visible without using PPV to classify fluid response.

**2026-08-21 — Plethoric IVC no longer freezes at a display ceiling.** The tamponade preset already retained an underlying respiratory IVC-volume swing, but the thorax panel clipped every value above 150 mL to the same width. Displayed calibre now follows the square root of distending volume and can continue above the reference dilated width, preserving a small excursion without altering the circulation. The IVC, thorax-panel, tamponade and scenario-validation pages now describe a dilated, less collapsible conduit rather than a universally fixed one; the numerical comparison is generated from the current model and anchored to Himelman et al. as a directional, not calibrated, reference.

**2026-08-21 — Guyton points defined by quantity and timescale.** The manual now states that the filled point is one-heartbeat mean right atrial pressure against IVC-to-right-atrial venous inflow, not cardiac output, while the hollow point is a local analytic steady-state crossing. A simple right-heart volume balance explains why venous inflow and RV output may diverge within a breath. The pulmonary-embolism example now separates physiological storage and delayed transmission from a second cause of point separation: reduced fidelity of the simplified analytic cardiac curve during severe RV pressure loading.

**2026-08-21 — The Guyton ascending curve identified as right-ventricular.** Conventional “cardiac-function curve” shorthand concealed that `cardiacFunctionCurve` uses right atrial filling, the RV diastolic relation, RV contractility and current effective pulmonary arterial load. The panel now labels it **RV function**, and the manual reserves systemic cardiac output for the whole-circuit steady-state equality of venous return, RV output and LV output. The preload-reserve page now states the resulting boundary: its analytic slope predicts right-sided steady flow and does not independently test LV reserve.

**2026-08-21 — Literal clinical language made an editorial rule.** The PVR state-descriptor table now says directly that inflation and deflation points fall approximately on the same curve, replacing “approach one relation”. The schema now requires observable statements rather than elegant abstractions, while retaining scientific uncertainty through explicit qualifiers.

**2026-08-21 — Scenario construction made visible and executable.** The scenario page now states that every preset starts from `defaultParams()` rather than inheriting the opening healthy-spontaneous state or the previously selected patient. A generated table for each active preset lists every effective control change with its domain, reference value and preset value; unchanged declarations are omitted. The same source that defines the scenarios regenerates the tables, so adding or changing an override makes manual validation fail until the documentation is updated.
